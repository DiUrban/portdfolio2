/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["design-style-guide.freecodecamp.org", "cdn.sanity.io"],
  },
};

module.exports = nextConfig;
